[package]
name = "rustls-postquantum"
version = "0.1.0"
edition = "2021"

[dependencies]
rustls = { path = "../rustls", features = [ "aws_lc_rs" ]}
aws-lc-rs = "1.5"

[dev-dependencies]
env_logger = "0.10"
webpki-roots = "0.26"
